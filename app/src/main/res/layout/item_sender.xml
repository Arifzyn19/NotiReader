<?xml version="1.0" encoding="utf-8"?>
<carbon.widget.RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:background="@color/whatsapp_dark_green_trans"
    android:paddingHorizontal="12dp"
    card_view:carbon_cornerRadius="10dp">

    <carbon.widget.ImageView
        android:id="@+id/profilePicture"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="@color/whatsapp_dark_green"
        android:contentDescription="profilePicture"
        android:padding="12dp"
        android:src="@drawable/ic_user"
        card_view:carbon_cornerRadius="20dp"
        card_view:carbon_tint="@color/whatsapp_primary_green" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/profilePicture"
        android:orientation="vertical">

        <TextView
            android:id="@+id/senderName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:gravity="center_vertical"
            android:maxLines="1"
            android:paddingHorizontal="12dp"
            android:paddingTop="16dp"
            android:paddingBottom="6dp"
            android:text="senderName"
            android:textColor="@color/warm_white"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/latestMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:gravity="center_vertical"
            android:maxLines="1"
            android:paddingHorizontal="12dp"
            android:paddingBottom="14dp"
            android:text="latestMessage"
            android:textColor="@color/muted_silver"
            android:textSize="12sp" />
    </LinearLayout>

    <carbon.widget.TextView
        android:id="@+id/latestTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:paddingHorizontal="10dp"
        android:paddingTop="10dp"
        android:paddingBottom="6dp"
        android:text="latestTime"
        android:textColor="@color/muted_silver"
        android:textSize="12sp" />

    <carbon.widget.TextView
        android:id="@+id/numberOfUnReads"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_below="@id/latestTime"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="12dp"
        android:background="@color/whatsapp_primary_green"
        android:gravity="center"
        android:text="1"
        android:textColor="@color/deep_charcoal"
        android:visibility="gone"
        card_view:carbon_cornerRadius="12dp" />

</carbon.widget.RelativeLayout>
